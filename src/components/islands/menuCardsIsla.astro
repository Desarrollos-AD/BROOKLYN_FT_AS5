---
import CardsMenu from "../ui/cards.astro";
import CardsRefrescos from "../ui/cardsRefrescos.astro";
import CardsBurger from "../ui/cardsBurger.astro";
import CardsSancks from "../ui/CardsSnacks.astro";
import CardsGrill from "../ui/cardsGrill.astro";
---

<div class="menuCards contenedor">
  <h3 class="h3_linea--decoraccion">Nuestro menú</h3>
  <nav class="menuCards__tabs">
    <button type="button" data-paso="1">Burgers</button>
    <button type="button" data-paso="2">Refrescos</button>
    <button class="actual" type="button" data-paso="3">Snacks</button>
    <button type="button" data-paso="4">Grill</button>
    <!-- <button type="button" data-paso="2">Munchies</button> -->
    <!-- <button type="button" data-paso="5">Soft drinks</button> -->
    <!-- <button type="button" data-paso="7">Postres</button> -->
  </nav>

  <div id="paso-1" class="menuCards__tabs--seccion">
    <CardsBurger />
  </div>
  <div id="paso-2" class="menuCards__tabs--seccion">
    <CardsRefrescos />
  </div>
  <div id="paso-3" class="menuCards__tabs--seccion">
    <div class="">
      <CardsSancks />
    </div>
  </div>
  <div id="paso-4" class="menuCards__tabs--seccion">
    <CardsGrill />
  </div>
  <!-- <div id="paso-5" class="menuCards__tabs--seccion">
    <h2>Soft drinks</h2> 
    <CardsMenu />
  </div>-->
  <!-- <div id="paso-2" class="menuCards__tabs--seccion">
    <h2>Munchies</h2> 
    <CardsMenu />
    </div>-->
  <!-- <div id="paso-7" class="menuCards__tabs--seccion">
          <h2>Postres</h2> 
          <CardsMenu />
          </div>-->
</div>

<script is:client="load">
  let paso = 1;

  document.addEventListener("DOMContentLoaded", function () {
    iniciarApp();
  });

  function iniciarApp() {
    mostrarSeccion(); // Muestra y oculta las secciones
    tabs(); // Cambia la sección cuando se presionen los tabs
  }

  function mostrarSeccion() {
    // Ocultar la sección que tenga la clase de mostrar
    const seccionAnterior = document.querySelector(".mostrar");
    if (seccionAnterior) {
      seccionAnterior.classList.remove("mostrar");
    }

    // Seleccionar la sección con el paso...
    const pasoSelector = `#paso-${paso}`;
    const seccion = document.querySelector(pasoSelector);
    seccion.classList.add("mostrar");

    // Quita la clase de actual al tab anterior
    const tabAnterior = document.querySelector(".actual");
    if (tabAnterior) {
      tabAnterior.classList.remove("actual");
    }

    // Resalta el tab actual
    const tab = document.querySelector(`[data-paso="${paso}"]`);
    tab.classList.add("actual");
  }

  function tabs() {
    // Agrega y cambia la variable de paso según el tab seleccionado
    const botones = document.querySelectorAll(".menuCards__tabs button");
    botones.forEach((boton) => {
      boton.addEventListener("click", function (e) {
        e.preventDefault();

        paso = parseInt(e.target.dataset.paso);
        mostrarSeccion();
      });
    });
  }
</script>
