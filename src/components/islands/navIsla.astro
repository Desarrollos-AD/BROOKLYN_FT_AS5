---

---

<nav class="nav">
  <div class="contenedor">
    <div class="nav__barra">
      <div class="nav__barra--logo">
        <img src="/images/brooklyn.jpg" alt="Logo Brooklyn" loading="lazy" />
      </div>
      <div id="nav" class="nav__barra--enlaces">
        <div class="nav__barra--enlaces--links">
          <div class="nav__barra--logo-enlace">
            <img
              src="/images/brooklyn.jpg"
              alt="Logo Brooklyn"
              loading="lazy"
            />
          </div>

          <div class="nav__barra--enlaces--links-enlace activo">
            <a class="bebasNeueBold" href="#Home">Home</a>
          </div>
          <div class="nav__barra--enlaces--links-enlace">
            <a class="bebasNeueBold" href="#About">About</a>
          </div>
          <div class="nav__barra--enlaces--links-enlace">
            <a class="bebasNeueBold" href="#Category">Category</a>
          </div>
          <div class="nav__barra--enlaces--links-enlace">
            <a class="bebasNeueBold" href="#Contact">Contact</a>
          </div>
          <div class="nav__barra--enlaces--links-enlace">
            <a class="bebasNeueBold" href="#Feedback">Feedback</a>
          </div>
        </div>
      </div>
      <button id="button-menu" class="button-menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </div>
</nav>

<script is:client="load">
  const toggleButton = document.getElementById("button-menu");
  const links = document.querySelectorAll("nav__barra--enlaces--links");
  const navMenu = document.getElementById("nav");

  toggleButton.addEventListener("click", () => {
    toggleButton.classList.toggle("close");
    navMenu.classList.toggle("show");
  });

  //Cierra menú al hacer clic fuera del menú
  navMenu.addEventListener("click", (e) => {
    if (e.target.id === "nav") {
      navMenu.classList.remove("show");
      toggleButton.classList.remove("close");
    }
  });

  // Cierra el menú cuando se hace clic en un enlace
  links.forEach((link) => {
    link.addEventListener("click", () => {
      nav.classList.remove("active");
      buttonMenu.classList.remove("active");
    });
  });
</script>
